const handleError = require('./handleError');
const validateToken = require('./validateToken');
const checkPostExistence = require('./checkPostExistence');

module.exports = {
  handleError,
  validateToken,
  checkPostExistence,
};
